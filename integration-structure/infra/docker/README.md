# infra/docker

Exemplos Docker Compose e configuração de redes para isolar serviços.

Incluir:
- `docker-compose.yml` (django, postgres, zeroclaw)
- `env.example`
- network settings (backend bridge)
